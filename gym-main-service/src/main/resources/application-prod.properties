# Production Environment Profile
# Note: Replace with actual production database configuration
spring.datasource.url=jdbc:postgresql://prod-db-host:5432/gymdb
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.defer-datasource-initialization=false
spring.sql.init.mode=never

# H2 Console
spring.h2.console.enabled=false

# Actuator Endpoints - Minimal exposure in production
management.endpoints.web.exposure.include=health,metrics,prometheus
management.endpoints.web.base-path=/actuator
management.endpoint.health.show-details=never

# Health Check
health.demo.required-users=John.Smith,Sarah.Williams
